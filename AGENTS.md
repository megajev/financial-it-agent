# AGENTS.md

이 파일은 AI 코딩 도구(Claude Code, Cursor, Copilot, Codex 등)가 이 프로젝트에서 코드를 작성·수정하기 전에 반드시 읽어야 하는 지침입니다.

---

## 1. 프로젝트 개요

금융회사 IT 부서를 위한 **일일 브리핑 대시보드** 서비스입니다. 사용자가 "브리핑 생성" 버튼을 누르면 다단계 Agent가 세 가지 작업을 자동 수행해 한 화면에 종합합니다.

- ① **정기 보고서 초안** — 사내 문서를 RAG로 검색해 초안 생성 (내부 정보)
- ② **시장·여론 요약** — 뉴스·웹을 수집해 요약 (외부 정보)
- ③ **고객 문의 동향** — 문의를 SLM으로 분류해 집계·트렌드 표시

각 섹션은 대시보드에 요약으로 표시되고, 상세보기로 근거·원문·개별 데이터를 확인합니다.

Furiosa NPU 부트캠프 팀 프로젝트이며, 개발 기간은 3.5일입니다.

---

## 2. 기술 스택

이 스택만 사용합니다. 임의로 다른 프레임워크·라이브러리를 도입하지 마세요.

- **언어**: Python (백엔드), 프론트는 프로젝트 초기 결정에 따름
- **백엔드**: FastAPI
- **에이전트 오케스트레이션**: LangGraph
- **검색**: RAG (임베딩 + 벡터 검색)
- **모델 추론**: Furiosa NPU API / NPU Pod 서빙
- **분류 모델**: 오픈소스 SLM (파인튜닝은 선택)
- **DB**: 결과 저장용 (구체 DB는 초기 결정에 따름)
- **프론트**: 대시보드 UI, 상세보기는 모달 또는 아코디언

---

## 3. 프로젝트 특유의 핵심 규칙 (반드시 준수)

> 아래 규칙은 이 프로젝트의 설계 결정입니다. 위반하는 코드를 생성하지 마세요.

1. **NPU는 모델 추론에만 사용한다.**
   검색·크롤링·DB·API·오케스트레이션 등은 일반 서버 로직으로 처리하고, LLM 생성·SLM 분류 등 **모델이 언어를 생성·분류하는 추론만 Furiosa NPU를 통해** 실행한다.

2. **결과는 생성 시 전체를 만들어 저장하고, 상세보기에서는 재사용한다.**
   브리핑 생성 시 전체 결과(보고서 전문, 전체 뉴스 목록, 개별 분류 결과)를 만들어 DB에 저장한다. 대시보드에는 요약만 표시한다. **상세보기 요청 시 모델을 다시 호출하지 말고 저장된 결과를 조회해 반환한다.** (응답 속도 + 요약/상세 근거 일관성)

3. **세 섹션의 데이터 출처를 혼동하지 않는다.**
   - ① 정기 보고서 = **사내** 모의 문서 (RAG)
   - ② 시장·여론 = **외부** 뉴스·웹
   - ③ 고객 문의 = 모의 문의 데이터 (SLM 분류)

4. **근거·출처를 항상 함께 저장·반환한다.**
   보고서·요약은 참조한 문서/원본 링크를 상세보기에서 확인할 수 있어야 한다. 확인되지 않은 인용·수치는 결과에 포함하지 않는다 (금융 컴플라이언스).

5. **실제 금융 데이터를 사용하지 않는다.**
   공개·모의 데이터만 사용하고, 개인정보는 비식별 처리한다.

---

## 4. 폴더 구조 (권장)

```
project/
├── app/
│   ├── main.py            # FastAPI 진입점
│   ├── api/               # 라우터 (엔드포인트)
│   ├── agents/            # LangGraph 그래프·노드 정의
│   ├── services/
│   │   ├── report/        # ① RAG 보고서
│   │   ├── news/          # ② 뉴스 수집·요약
│   │   └── inquiry/       # ③ 문의 분류·집계
│   ├── models/            # NPU 추론 클라이언트, SLM 래퍼
│   ├── db/                # 저장·조회
│   └── schemas/           # Pydantic 입출력 스키마
├── data/                  # 모의 사내 문서, 라벨링 문의 데이터
├── frontend/              # 대시보드 UI
├── tests/
├── AGENTS.md
└── README.md
```

> 세 기능(report/news/inquiry)은 서로 독립적으로 동작 가능해야 하며, 각 기능의 입출력은 `schemas/`에 정의된 형식을 따른다. Agent는 이 형식으로 각 서비스를 호출·조립한다.

---

## 5. 코딩 규칙

- **입출력 스키마 우선**: 기능 간 연결은 Pydantic 스키마로 명세한 뒤 구현한다. 각 서비스 함수는 정해진 입력/출력 형식을 지킨다.
- **네이밍**: 함수·변수는 snake_case, 클래스는 PascalCase, 상수는 UPPER_SNAKE_CASE.
- **모델 호출 분리**: NPU/모델 호출은 `models/` 안의 클라이언트를 통해서만 한다. 비즈니스 로직에 모델 호출을 직접 박지 않는다 (교체·비교 실험 대비).
- **설정 분리**: API 키·엔드포인트·모델명 등은 코드에 하드코딩하지 말고 환경변수/설정 파일로 관리한다.
- **에러 처리**: 외부 호출(NPU API, 웹 수집)은 실패 가능성을 가정하고 예외 처리한다.
- **주석**: 왜 그렇게 했는지(의도)를 중심으로 간결하게. 자명한 코드에는 주석을 달지 않는다.

---

## 6. 하지 말 것

- 스택에 없는 프레임워크·라이브러리 임의 도입 금지
- 상세보기에서 모델 재호출 금지 (저장분 재사용)
- 실제 금융 데이터·개인정보 사용 금지
- API 키·비밀값 하드코딩 및 커밋 금지
- 비즈니스 로직에 모델 호출 직접 삽입 금지 (클라이언트 경유)
- Docker / CI-CD / Langfuse 등 범위 외 요소를 요청 없이 추가 금지 (3.5일 스코프 보호)

---

## 7. 범위 우선순위

작업 요청이 모호할 때는 Must-have를 우선한다.

**Must-have**: 대시보드+생성 버튼 / ① RAG 보고서(+출처) / ② 여론 요약(+원문 링크) / ③ 문의 분류·집계·드릴다운 / 상세보기 / NPU 추론 연동

**Nice-to-have (요청 시에만)**: SLM 파인튜닝·NPU 직접 서빙 / SLM vs API 비교 / 자동 스케줄링 / 통합 채팅+자동 라우팅

**Out of scope**: 실제 데이터 연동 / Docker·CI-CD·Langfuse / 운영 배포

---

## 8. 실행·테스트 (초기 세팅 후 업데이트)

```bash
# 의존성 설치
pip install -r requirements.txt

# 개발 서버 실행
uvicorn app.main:app --reload

# 테스트
pytest
```

> 구체 명령은 프로젝트 세팅이 확정되면 이 섹션을 갱신한다.
